"""
    def func(x):
    ““”
    函数说明
    :param x：形参x的说明
    :return: 返回值的说明
    ““”
    函数体
    return
"""


def func2(x, y):
    """
    函数说明
    add函数可以接受两个参数，进行两数相加的功能
    :param x: 形参x表示相加的其中一个数字
    :param y: 形参y表示相加的另一个数字
    :return:  返回值是两数字相加的结果
    """
    result = x + y
    print(f"两数的结果为:{result}")
    return result


func2(1, 2)
