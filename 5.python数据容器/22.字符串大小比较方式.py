"""
    ASCII表：字符串所用到的字符均有对应的ASCII码表值
            字符串进行比较就是基于数字的ASCII码值大小进行比较的
    字符串比较：按位比较，一位位比较，只要有一位大，那么整体就大
"""
print(f"abd大于abc，结果为{'abd' > 'abc'}")
print(f"a大于ab，结果为{'a' > 'ab'}")
print(f"a大于A，结果为{'a' > 'A'}")
